name: Analyze PR
on:
  pull_request:
permissions:
  pull-requests: write
jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: ./.github/actions/Descriptor
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}